---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout pageTitle="URL Encoder / Decoder">
  <div class="max-w-4xl mx-auto px-4 min-h-[60vh]">
    <div class="text-center mb-10 pt-8 animate-on-scroll">
      <h1 class="text-3xl md:text-4xl font-extrabold mb-3 text-transparent bg-clip-text bg-gradient-to-r from-teal-500 to-emerald-500">
        URL Encoder / Decoder
      </h1>
      <p class="text-slate-600 dark:text-slate-400">
        Encode URL agar aman digunakan atau Decode URL yang berantakan (%20).
      </p>
    </div>

    <div class="bg-white dark:bg-slate-800 p-1 rounded-xl inline-flex mb-8 border border-slate-200 dark:border-slate-700 animate-fade-in mx-auto relative left-1/2 -translate-x-1/2">
       <button id="mode-encode" class="px-6 py-2 rounded-lg text-sm font-bold bg-teal-500 text-white shadow-md transition-all">Encoder</button>
       <button id="mode-decode" class="px-6 py-2 rounded-lg text-sm font-medium text-slate-500 hover:text-slate-700 dark:text-slate-400 transition-all">Decoder</button>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 animate-fade-in">
      
      <div class="flex flex-col h-full bg-white dark:bg-slate-800 rounded-xl shadow-lg border border-slate-200 dark:border-slate-700 overflow-hidden">
         <div class="bg-slate-100 dark:bg-slate-900 px-4 py-2 border-b border-slate-200 dark:border-slate-700 flex justify-between items-center">
            <span class="text-xs font-bold text-slate-500 uppercase">Input</span>
            <button id="clear-btn" class="text-xs font-bold text-red-500 hover:text-red-600">Clear</button>
         </div>
         <textarea id="input-text" class="flex-1 w-full p-4 font-mono text-sm bg-slate-50 dark:bg-slate-900 text-slate-800 dark:text-slate-200 outline-none resize-none min-h-[200px]" placeholder="https://example.com/search?q=hello world"></textarea>
      </div>

      <div class="flex flex-col h-full bg-white dark:bg-slate-800 rounded-xl shadow-lg border border-slate-200 dark:border-slate-700 overflow-hidden">
         <div class="bg-slate-100 dark:bg-slate-900 px-4 py-2 border-b border-slate-200 dark:border-slate-700 flex justify-between items-center">
            <span class="text-xs font-bold text-slate-500 uppercase">Result</span>
            <button id="copy-btn" class="text-xs font-bold text-teal-600 hover:text-teal-700">Copy</button>
         </div>
         <textarea id="output-text" readonly class="flex-1 w-full p-4 font-mono text-sm bg-slate-50 dark:bg-slate-900 text-slate-800 dark:text-slate-200 outline-none resize-none min-h-[200px]" placeholder="https://example.com/search?q=hello%20world"></textarea>
      </div>

    </div>
  </div>
</BaseLayout>

<script>
  const input = document.getElementById('input-text');
  const output = document.getElementById('output-text');
  const modeEncode = document.getElementById('mode-encode');
  const modeDecode = document.getElementById('mode-decode');
  const clearBtn = document.getElementById('clear-btn');
  const copyBtn = document.getElementById('copy-btn');

  let isEncode = true;

  function process() {
    const val = input.value;
    if(!val) { output.value = ""; return; }

    try {
        if (isEncode) {
            output.value = encodeURIComponent(val).replace(/%20/g, '+'); // Standard URL encoding
        } else {
            output.value = decodeURIComponent(val.replace(/\+/g, '%20'));
        }
    } catch (e) {
        output.value = "Error: Invalid URL format";
    }
  }

  function setMode(encode) {
    isEncode = encode;
    
    if (encode) {
        modeEncode.className = "px-6 py-2 rounded-lg text-sm font-bold bg-teal-500 text-white shadow-md transition-all";
        modeDecode.className = "px-6 py-2 rounded-lg text-sm font-medium text-slate-500 hover:text-slate-700 dark:text-slate-400 transition-all";
    } else {
        modeDecode.className = "px-6 py-2 rounded-lg text-sm font-bold bg-teal-500 text-white shadow-md transition-all";
        modeEncode.className = "px-6 py-2 rounded-lg text-sm font-medium text-slate-500 hover:text-slate-700 dark:text-slate-400 transition-all";
    }
    process();
  }

  modeEncode.addEventListener('click', () => setMode(true));
  modeDecode.addEventListener('click', () => setMode(false));
  input.addEventListener('input', process);
  
  clearBtn.addEventListener('click', () => {
    input.value = ""; output.value = ""; input.focus();
  });

  copyBtn.addEventListener('click', () => {
    if(!output.value) return;
    navigator.clipboard.writeText(output.value);
    window.showToast("URL disalin!", "success");
  });
</script>
